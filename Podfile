# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NTT' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NTT
   pod 'Alamofire', '~> 5.5'
   pod 'SwiftyJSON', '~> 4.0'
   pod 'SDWebImage', '~> 5.0'
   pod 'SwiftMessages'

  target 'NTTTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NTTUITests' do
    # Pods for testing
  end

end
